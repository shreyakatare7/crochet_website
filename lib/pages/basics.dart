import 'package:flutter/material.dart';

class Basics extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Learn the Basics')),
      body: Center(child: Text('Learn the Basics Page')),
    );
  }
}
