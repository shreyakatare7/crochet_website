import 'package:flutter/material.dart';

class Patterns extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Patterns')),
      body: Center(child: Text('Patterns Page')),
    );
  }
}
